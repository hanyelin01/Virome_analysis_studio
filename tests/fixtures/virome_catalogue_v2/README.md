# v2 回归测试数据契约

此目录仅保存人工构造的最小 FASTA、TSV 和预期结果，不含任何真实 reads、样本名称、宿主信息或参考数据库。

它覆盖 v2 的核心数据契约：两个来源样本的 contig 在三种发现证据下合并为 VC，完整 NR 分类后仅一个候选进入 CheckV/ICTV，最终生成可回溯的 VF 并回分发到原始样本。真实 geNomad、VirSorter2、CheckV、DIAMOND、CoverM 由独立的环境诊断和真实小批次验收测试覆盖；它们不应被伪造的微型数据替代。

修改本目录的预期输出前，必须在提交说明中解释是经审核的科学规则变更，还是修复了旧行为。
